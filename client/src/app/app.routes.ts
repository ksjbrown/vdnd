import { Routes } from '@angular/router';
import { CharacterList } from './features/characters/character-list/character-list';

export const routes: Routes = [
  { path: "characters", component: CharacterList }
];
